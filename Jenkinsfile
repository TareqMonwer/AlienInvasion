pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                script {
                    // Ensure Python3 is installed and check its version
                    sh 'python3 --version'
                }
            }
        }
    }
}
